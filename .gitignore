/target

# Local Claude Code working state (machine-specific, not part of the project)
.claude/worktrees/
.claude/*.lock
.claude/settings.local.json

# Working documents (Loro snapshots) — never commit a user's file
*.strop
*.strop.tmp
target-main/
